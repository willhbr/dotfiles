export EDITOR=vim
export PROJECT_PATHS="$HOME/projects"

export PATH="$HOME/.local/bin:$PATH:$HOME/.local/df-bin:$HOME/.cargo/bin"

if [ "$(uname)" = Darwin ]; then
  export CLICOLOR=1
else
  eval "$(dircolors -b ~/.dircolors)"
fi
