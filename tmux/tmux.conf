set-window-option -g mode-keys vi
set -g status-position bottom
set -g base-index 1
set -g default-terminal "screen-256color"

bind c new-window -c "#{pane_current_path}"
bind -n M-c new-window -c "#{pane_current_path}"

# split panes using h and v
bind v split-window -h -c "#{pane_current_path}"
bind h split-window -v -c  "#{pane_current_path}"
unbind '"'
unbind %

bind V move-pane -h
bind H move-pane -v

# Set prefix to C-z
unbind C-b
set -g prefix C-z
bind C-z send-prefix

bind -n M-n next-window
bind -n M-m previous-window

bind -n M-h resize-pane -L
bind -n M-j resize-pane -D
bind -n M-k resize-pane -U
bind -n M-l resize-pane -R

bind r source-file ~/.tmux.conf

set -g pane-border-fg black

## Status bar design

set -g status-justify left
set -g status-bg default
set -g status-interval 2

# loud or quiet?
set-option -g visual-activity off
set-option -g visual-bell off
set-option -g visual-silence off
set-window-option -g monitor-activity off
set-option -g bell-action none

# The modes {
setw -g clock-mode-colour "colour$HOST_COLOR"
setw -g mode-attr bold
setw -g mode-fg colour135
setw -g mode-bg colour235

# The panes {

set -g pane-border-fg colour238
set -g pane-active-border-fg colour252 # 229 # Yellow-ish

# The statusbar {

set -g status-interval 60
set -g status-bg colour234
set -g status-fg colour137
set -g status-left ''
set -g status-right "#[fg=colour245]%H:%M (%a %e) #[fg=colour0,bg=colour$HOST_COLOR,bold] #{session_name} on #T"
set -g status-right-length 50

# Current window tab
setw -g window-status-current-fg colour229 # Yellow-ish
setw -g window-status-current-bg colour238
setw -g window-status-current-attr bold
setw -g window-status-current-format ' #I#[fg=colour250]:#[fg=colour255]#W#[fg=colour73]#F '

# Other window tabs
setw -g window-status-fg colour30
setw -g window-status-bg colour235
setw -g window-status-attr none
setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '

# The messages {

set -g message-attr bold
set -g message-fg colour232
set -g message-bg "colour$HOST_COLOR"

# }

source-file ~/.pug/source/tmux/pug
if-shell "[ -e ~/.tmux-local.conf ]" 'source-file ~/.tmux-local.conf'
